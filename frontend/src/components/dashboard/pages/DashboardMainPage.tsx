import React from "react";
import { Box } from "@chakra-ui/react";
import { StatCardBlock } from "../Components/PrimaryStatCard";

export const Dashboard: React.FC = () => {
  return (
    <Box>
      <StatCardBlock />
    </Box>
  );
};
